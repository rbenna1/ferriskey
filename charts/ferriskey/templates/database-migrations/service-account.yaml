{{- if .Values.databaseMigrations.serviceAccount.create }}
{{- $annotations := merge dict .Values.common.serviceAccount.annotations .Values.databaseMigrations.serviceAccount.annotations -}}
{{- $labels := merge dict .Values.common.serviceAccount.labels .Values.databaseMigrations.serviceAccount.labels -}}
{{- $automountServiceAccountToken := include "ferriskey.util.bool" (dict "value" .Values.databaseMigrations.serviceAccount.automountServiceAccountToken "default" .Values.common.serviceAccount.automountServiceAccountToken) -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ferriskey.databaseMigrations.serviceAccountName" . }}
  {{- with $annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "ferriskey.databaseMigrations.labels" . | nindent 4 }}
    {{- with $labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
{{- if ne $automountServiceAccountToken "" }}
automountServiceAccountToken: {{ $automountServiceAccountToken }}
{{- end }}
{{- end }}
